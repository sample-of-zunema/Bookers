<p id="notice">
    <%= notice %>
</p>

<table>
    <tr>
        <td>Title
            <p><%= @book.title %></p>
        </td>
        <td>Body
            <p><%= @book.body %></p>
        </td>
    </tr>
</table>


<%= link_to "edit", edit_book_path(@book) %>
<%= link_to "Back", books_path(@book) %>